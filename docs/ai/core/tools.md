## Module Purpose
This file defines a `ToolSelector` for identifying and processing tool requests, along with a `BaseTool` class and concrete tool implementations like `FileLister` for directory operations and `OpenWeatherAPI` for weather data retrieval.

## Interface & Exports
*   `ToolSelector` (class)
*   `BaseTool` (class)
*   